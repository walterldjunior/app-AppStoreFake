# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'app-AppStoreFake' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for app-AppStoreFake
    pod 'SDWebImage', '~> 5.0'
  target 'app-AppStoreFakeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'app-AppStoreFakeUITests' do
    # Pods for testing
  end

end
